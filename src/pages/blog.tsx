import { HeadLine } from '@components/typography';
import { Container } from 'react-bootstrap';

const Blog = () => {
  return (
    <Container>
      <HeadLine size={1}>Blog</HeadLine>
    </Container>
  );
};

export default Blog;
