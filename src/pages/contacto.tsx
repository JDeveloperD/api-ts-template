import { HeadLine } from '@components/typography';
import { Container } from 'react-bootstrap';

const Contact = () => {
  return (
    <Container>
      <HeadLine size={1}>Contacto</HeadLine>
    </Container>
  );
};

export default Contact;
